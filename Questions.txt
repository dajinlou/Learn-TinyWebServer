1 在Utils类中，int *Utils::u_pipefd = 0;没有申请内存，为什么就直接使用了呢？

2 感觉线程池写的太简单了，应该有个管理者负责管理线程


3 遇到的困难
    1) 程序刚写完，运行./server后，没有报错，但是没有日志输出。
        经过手动打印日志，发现程序被阻塞了，加锁之后，本应解锁，反而再次加锁
    2) 再次运行报"段错误",发现日志的文件描述符空指针，继续追踪，发现
    创建文件时失败，创建失败的原因和名字有关。(包含特殊字符)
    3) 程序再次启动，无报错，日志也有输出，但浏览器一直连不上，查看打印日志
    发现可以收到客户端请求，不知怎地，去不回复响应。